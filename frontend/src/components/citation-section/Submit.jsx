const Button = () => {
  return (
    <button
      type="submit"
      className="rounded-md bg-black px-3 text-sm font-semibold text-white  hover:bg-indigo-500"
    >
      Submit
    </button>
  );
};

export default Button;
