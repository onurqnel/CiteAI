# Import the FastAPI class to create the application instance

# Import CORS middleware to allow communication between frontend and backend (different origins)

# Import configuration variables (such as API keys or allowed origins) from the core module

# Import the API router from the api module to connect endpoints to the main app

# Create a FastAPI app instance

# Add CORS middleware to the app with allowed origins, methods, and headers

# Include the API router under a specific URL prefix (e.g., "/api")
